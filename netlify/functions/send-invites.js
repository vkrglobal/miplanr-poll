const {json}=require('./_supabase');
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(200,{});try{const key=process.env.RESEND_API_KEY;if(!key)return json(200,{ok:false,message:'RESEND_API_KEY not set'});const b=JSON.parse(event.body||'{}');const poll=b.poll||{};const recipients=b.recipients||[];for(const email of recipients){const url=(process.env.URL||'https://miplanr.com')+'/poll/'+poll.slug;await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({from:'miPlanr <poll@mail.miplanr.com>',to:email,subject:'🗳️ Please vote in this miPlanr poll',html:`<div style="font-family:Arial;padding:24px"><h2>${poll.icon||'✨'} ${poll.title||poll.question}</h2><p>${poll.question||''}</p><p><a style="background:#0072FF;color:#fff;padding:14px 18px;border-radius:12px;text-decoration:none" href="${url}">Vote now</a></p><p>Dream • Plan • Soar</p></div>`})});}return json(200,{ok:true});}catch(e){return json(500,{error:e.message})}}
