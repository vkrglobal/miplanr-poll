const { client } = require('./_supabase');
const crypto = require('crypto');
const slug=()=>crypto.randomBytes(5).toString('hex');
const token=()=>crypto.randomBytes(12).toString('hex');
exports.handler=async(event)=>{try{const body=JSON.parse(event.body||'{}');const sb=client();const poll={slug:slug(),title:body.title,question:body.question,description:body.description||'',creator:body.creator||'',location:body.location||'',start_at:body.start_at||null,end_at:body.end_at||null,deadline_at:body.deadline_at||null,threshold:body.threshold||3};const {data:p,error:e}=await sb.from('polls').insert(poll).select('*').single();if(e)throw e;const options=(body.options||[]).map((o,i)=>({poll_id:p.id,option_text:o,sort_order:i}));if(options.length){const {error:oe}=await sb.from('poll_options').insert(options);if(oe)throw oe;}const emails=[...new Set((body.emails||[]).filter(Boolean).map(x=>x.toLowerCase()))];const participants=emails.map(email=>({poll_id:p.id,email,invite_token:token()}));if(participants.length){const {data:ps,error:pe}=await sb.from('participants').insert(participants).select('*');if(pe)throw pe;await sendInvites(p,ps||[]);}return {statusCode:200,body:JSON.stringify({slug:p.slug,id:p.id})}}catch(err){return {statusCode:500,body:JSON.stringify({error:err.message})}}}
async function sendInvites(p,participants){if(!process.env.RESEND_API_KEY||!participants.length)return;const origin=process.env.URL||'https://miplanr.com';await Promise.all(participants.map(async part=>{const link=`${origin}/poll.html?slug=${encodeURIComponent(p.slug)}&invite=${encodeURIComponent(part.invite_token)}`;await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${process.env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({from:'miPlanr <poll@mail.miplanr.com>',to:part.email,subject:`🗳️ ${p.creator||'Someone'} invited you to vote: ${p.title}`,html:`<div style="font-family:Arial,sans-serif;max-width:620px;margin:auto"><h1>🗳️ ${p.title}</h1><p>${p.question}</p><p><b>Location:</b> ${p.location||'TBC'}</p><p><a style="background:#0072FF;color:white;padding:12px 18px;border-radius:10px;text-decoration:none" href="${link}">Vote now</a></p><p style="color:#667">Powered by miPlanr — Dream • Plan • Soar</p></div>`})})}))}
